{
	"name": "calendar-note-integration",
	"version": "6.5.0",
	"description": "Obsidian plugin: create structured notes from Apple Calendar, Google Calendar (iCal or OAuth), or any iCal feed",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json package.json",
		"version:patch": "node version-bump.mjs patch && git add manifest.json versions.json package.json",
		"version:minor": "node version-bump.mjs minor && git add manifest.json versions.json package.json",
		"version:major": "node version-bump.mjs major && git add manifest.json versions.json package.json"
	},
	"keywords": [
		"obsidian",
		"google-calendar",
		"notes",
		"productivity"
	],
	"author": "Brian Pavane",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^18.0.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.17.3",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
